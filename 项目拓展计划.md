# 项目拓展计划

基于现有学生端 + 管理员端的双角色架构，规划后续迭代方向。

## 一、维修工角色（最高优先级）

当前缺口：维修工借用管理员账号操作，权限过大且无法追踪到具体个人。

### 实现方案
- **新增角色** `role=2`（维修工），复用学生端基础页面结构
- **维修工视图**：仅展示分配给自己的报修单
- **操作限制**：仅允许"接单 → 维修中 → 完成维修"三个动作
- **管理员增加派单功能**：`repair` 表新增 `worker_id` 字段，管理员将报修单指派给指定维修工

### 涉及变更
- `user` 表 role 枚举扩展
- `repair` 表新增 `worker_id`，关联 user 表
- 新增维修工专属页面（接单列表、维修记录）
- 管理员页面增加派单组件

## 二、移动端适配

当前 Element UI 布局在移动端体验较差，而报修场景天然适合手机操作。

### 优先级
1. **维修工移动端** — 现场接单、拍照上传维修前后对比（小程序或 H5）
2. **学生移动端** — 提交报修、拍照、查看进度

## 三、报修流程精细化

### 状态流转扩展

```
待审核 → 待派单 → 已派单 → 维修中 → 待验收 → 已完成 → 已评价
```

### 数据价值
- 每个节点记录时间戳
- 可计算：平均响应时间、平均维修时长、维修工效率排名
- 为统计页面提供更丰富的数据维度

## 四、通知体系升级

当前已实现 WebSocket 实时推送，可进一步扩展：

- **超时预警** — 报修超过 N 小时未处理，自动通知管理员
- **评价提醒** — 维修完成后到期未评价，提醒学生
- **短信/邮件通知** — 维修工未接单时自动提醒

## 五、数据统计分析

在现有 Stats.vue 基础上，利用精细化流程数据实现：

- 各楼栋报修热点图
- 故障类型分布分析（电路/水管/门窗等）
- 维修工负荷排行
- 高峰期预测（开学季、雨季等）

## 六、潜在拓展方向

- **备件库存管理** — 维修所需零件的库存追踪
- **SaaS 化** — 从宿舍场景扩展到教学楼、办公楼等物业报修场景
- **自动派单** — 按楼栋区域、故障类型匹配维修工技能，实现智能派单

## 建议迭代路线

```
第一阶段 → 维修工角色 + 派单功能（核心缺口）
第二阶段 → 维修工移动端 H5 适配
第三阶段 → 报修流程精细化管理 + 时间戳记录
第四阶段 → 通知升级 + 统计增强
```
